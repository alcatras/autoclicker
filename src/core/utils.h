#ifndef AUTOCLICKER_UTILS_H
#define AUTOCLICKER_UTILS_H

#include <sstream>
#include <memory>
#include <string.h>

using namespace std;

int getRootWindowInfo(string grep);

int screenWidth();

int screenHeight();

#endif